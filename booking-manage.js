(function(){
  'use strict';
  const settings=window.BK_SETTINGS||{};const params=new URLSearchParams(location.search);const form=document.getElementById('manageForm');const button=document.getElementById('manageSubmit');const status=document.getElementById('manageStatus');document.getElementById('manageReference').value=params.get('ref')||'';document.getElementById('manageEmail').value=params.get('email')||'';
  form.addEventListener('submit',async(event)=>{event.preventDefault();if(!form.checkValidity()){form.reportValidity();return;}button.disabled=true;button.textContent='Sending…';status.textContent='Sending your request.';const data=new FormData(form);data.append('submittedAt',new Date().toISOString());data.append('page',location.href);try{const response=await fetch(settings.formEndpoint,{method:'POST',body:data,headers:{Accept:'application/json'}});if(!response.ok)throw new Error('Not accepted');form.reset();button.textContent='Request sent';status.textContent='Your change request was received. The original booking remains unchanged until you receive a reply.';}catch(_){button.disabled=false;button.textContent='Try again';status.innerHTML=`The request could not be sent. Please try again or email <a href="mailto:${settings.businessEmail}">${settings.businessEmail}</a>.`;}});
}());
